openapi: 3.1.0
info:
  version: 1.0.0
  title: Phonebook API
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    Contact management service
tags:
  - name: Contacts
    description: Operation with contacts
servers:
  - url: http://localhost:3000
  - url: https://your-deployed-app.com #исправить
paths:
  /contacts/{contactId}:
    get:
      $ref: ./swagger/paths/contacts/{id}/get.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT